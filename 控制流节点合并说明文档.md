这里为您生成了修正后的、严格符合您要求的完整 Markdown 文档。
为了方便您下载和使用，我将整个文档内容包裹在下面的 Markdown 代码块中。您可以直接点击代码块右上角的 “复制 (Copy)” 按钮，将其粘贴到一个新建的 .md 文件中（例如 ControlFlowFlattening.md），即可在任何支持 Markdown 的编辑器（如 Typora, VS Code, Obsidian 等）中完美查看和渲染流程图。
code
Markdown
# JS 控制流平坦化与节点合并算法

在 JavaScript 逆向工程与代码反混淆中，**控制流平坦化（Control Flow Flattening）** 是一种将原本线性的代码块拆分为多个离散的节点，并通过 `for` / `while` 循环配合 `switch` 语句（分发器）来打乱执行顺序的技术。

为了实现控制流的还原（即节点合并算法），我们首先需要对 `switch` 下的 `case` 节点进行严格的特征分类。以如下典型的平坦化代码为例：

## 样本代码

```javascript
for (var i, pc = 6; pc;) switch (pc) {
    case 1:// 单向节点
        i = 0;
        pc = 4;
        break;
    case 2:// 单向节点
        console.log(i);
        pc = 7;
        break;
    case 3:// 终止节点
        console.log("end");
        pc = 0; // pc为0时, 即退出循环，即为return；可将pc=0;改为return;
        break;
    case 4:// 分支节点
        pc = i < 6 ? 2 : 3;
        break;
    case 5:// 单向节点
        pc = 4;
        break;
    case 6:// 单向节点
        console.log("start");
        pc = 1;
        break;
    case 7:// 单向节点
        i++;
        pc = 5;
        break;
}
根据上述代码，我们将控制流节点严格划分为以下三种类型。注意：只有严格符合对应AST结构和语句特征的 case 块，才能被判定为该类型。
1. 单向节点 (One-way Node)
严格定义与特征：
语句构成：可以包含任意数量（0个或多个）的其他普通业务语句。
状态转移：在 case 块的最后，必须是对控制流指针 pc 的单一常量赋值（形如 pc = 常量;）。
结尾特征：必须严格以 break; 语句作为该 case 的结尾。
符合该类型的 Case：case 1, case 2, case 5, case 6, case 7。
节点流程图：
code
Mermaid
graph TD
    A([进入 case]) --> B[执行 0个或多个 业务语句]
    B --> C[单向赋值: pc = 常量<br/>例如: pc = 4]
    C --> D(break 跳出 switch)
    D --> E((进入下一轮 for 循环))
    
    style C fill:#d4edda,stroke:#28a745,stroke-width:2px
2. 分支节点 (Branching Node)
严格定义与特征：
语句构成：该 case 块内部绝对不允许包含任何其他业务语句。
状态转移：仅允许包含一条严格的三元运算赋值语句，根据条件将 pc 指向两个不同的常量（形如 pc = 条件 ? 常量1 : 常量2;）。
结尾特征：必须严格以 break; 语句作为该 case 的结尾。
符合该类型的 Case：case 4。
节点流程图：
code
Mermaid
graph TD
    A([进入 case]) --> B{严格执行三元运算<br/>pc = 条件 ? 常量1 : 常量2}
    B -- 条件成立 --> C[内部动作: pc = 常量1]
    B -- 条件不成立 --> D[内部动作: pc = 常量2]
    C --> E(break 跳出 switch)
    D --> E
    E --> F((进入下一轮 for 循环))

    style B fill:#fff3cd,stroke:#ffc107,stroke-width:2px
3. 终止节点 (Terminal Node)
严格定义与特征：
语句构成：同单向节点，可以包含任意数量（0个或多个）的最后阶段的业务语句。
状态转移：在 case 块的最后，必须将控制流指针 pc 赋值为导致外层循环退出的值（形如 pc = 0;）。如果直接使用 return; 退出函数，则替代赋值语句。
结尾特征：如果是通过 pc = 0; 退出的，则必须严格以 break; 语句结尾跳出 switch；如果是 return; 则直接结束。
符合该类型的 Case：case 3。
节点流程图：
code
Mermaid
graph TD
    A([进入 case]) --> B[执行 0个或多个 最后的业务语句]
    B --> C[阻断赋值: pc = 0 <br/> 或直接 return]
    C --> D(break 跳出 switch <br/> 若为return则直接跳出函数)
    D --> E(((彻底退出外层循环引擎)))

    style C fill:#f8d7da,stroke:#dc3545,stroke-width:2px
    style E fill:#e2e3e5,stroke:#383d41,stroke-width:2px
节点合并算法总结
在进行 AST (抽象语法树) 还原时，必须先通过脚本遍历所有的 SwitchCase 节点，运用上述严格的判定条件给每个 case 打上类型标签。
遇到 单向节点，意味着可以将下一个目标 case 的代码直接吸收到当前节点之后。
遇到 分支节点，则意味着控制流在此分叉，需要构建一个 IfStatement 或 WhileStatement 节点，并将两个目标常量对应的 case 代码分别填入分支结构中。
遇到 终止节点，表示当前执行路径到达尽头，控制流分析闭环。
严格遵守这三种节点结构的定义，是确保还原算法不出错、不遗漏业务代码的核心基础。
